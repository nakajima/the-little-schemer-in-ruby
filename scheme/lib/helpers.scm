; Get the paths straight
(define chapters-dir (path->string (simplify-path (build-path (current-directory) 'up "chapters"))))
(define lib-dir (path->string (simplify-path (build-path (current-directory) 'up "lib"))))